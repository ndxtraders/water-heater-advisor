/** Site-wide types inferred from the executable runtime content contract. */
export type {
  Branding,
  Business,
  ContentState,
  ConversionConfig,
  FooterConfig,
  GeoCoordinates,
  Navigation,
  NavigationLink,
  PostalAddress,
  SchemaConfig,
  SiteConfig,
} from "../lib/content-schema";
