# Repo root. Per-package ignores live alongside their package, e.g.
# site/.gitignore, whose leading-slash patterns anchor to site/ and keep
# working unchanged.

.DS_Store
*.pem

# Environment files, at any depth. Secrets never belong in the repo.
.env
.env.*
!.env.example
